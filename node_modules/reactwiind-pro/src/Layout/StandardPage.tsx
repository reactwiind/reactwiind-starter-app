import { Flex } from 'reactwiind';
import { StandardHeader, StandardHeaderProps, StandardFooter, StandardFooterProps } from "..";
import { FC, ReactNode } from 'react';

export interface StandardPageProps {
  header: StandardHeaderProps;
  footer: StandardFooterProps;
  children: ReactNode | Array<ReactNode> | null;
}

export const StandardPage:FC<StandardPageProps> = ({header, footer, children}: StandardPageProps) => {
    
  return <Flex data-component="component-standard-page" direction="flex-col">
     <StandardHeader {...header} /> 
     <>{children}</>
     {/* <StandardFooter {...footer}/> */}
  </Flex>
}